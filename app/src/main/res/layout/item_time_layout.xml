<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingTop="10dp"
    android:focusable="true"
    android:focusableInTouchMode="true"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal">
    <TextView
        android:id="@+id/time_tv"
        tools:text="时间"
        android:padding="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
    <TextView
        android:id="@+id/start_time_tv"
        android:layout_below="@id/time_tv"
        android:hint="开始时间"
        android:padding="10dp"
        android:gravity="center"
        android:inputType="number"
        android:layout_marginStart="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/time_tv"
        app:layout_constraintEnd_toStartOf="@id/time_divider_view"
        android:layout_width="0dp"
        style="@style/search_edit_style"/>
    <View
        android:id="@+id/time_divider_view"
        app:layout_constraintTop_toTopOf="@id/start_time_tv"
        app:layout_constraintBottom_toBottomOf="@id/start_time_tv"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="10dp"
        android:background="@color/common_gray_color_e9"
        android:layout_width="20dp"
        android:layout_height="2dp"/>
    <TextView
        android:id="@+id/end_time_tv"
        android:layout_below="@id/time_tv"
        android:hint="结束时间"
        android:gravity="center"
        android:layout_marginEnd="10dp"
        app:layout_constraintTop_toTopOf="@id/time_divider_view"
        app:layout_constraintBottom_toBottomOf="@id/time_divider_view"
        app:layout_constraintStart_toEndOf="@id/time_divider_view"
        app:layout_constraintEnd_toEndOf="parent"
        android:padding="10dp"
        android:layout_width="0dp"
        style="@style/search_edit_style"/>
</androidx.constraintlayout.widget.ConstraintLayout>