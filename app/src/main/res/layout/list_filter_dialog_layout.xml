<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/common_colorPrimary_light">

    <ScrollView
        android:id="@+id/filter_sv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="60dp"
        android:fillViewport="true"/>

    <Button
        android:id="@+id/reset_btn"
        android:layout_width="120dp"
        android:layout_height="40dp"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="15dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/shape_radius360_gray"
        android:text="重置"
        android:textColor="@color/common_colorPrimary"
        android:textSize="16sp" />

    <Button
        android:id="@+id/ensure_btn"
        android:layout_width="120dp"
        android:layout_height="40dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="15dp"
        android:layout_marginEnd="15dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/shape_radius360_primary"
        android:text="确定"
        android:textColor="@color/common_white"
        android:textSize="16sp" />
</RelativeLayout>